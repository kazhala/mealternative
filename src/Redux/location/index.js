import LocationReducer from './reducer';
import * as LocationTypes from './types';
import * as LocationActions from './actions';

export { LocationTypes, LocationActions };

export default LocationReducer;
