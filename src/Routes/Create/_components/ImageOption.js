import React from 'react';
import PropTypes from 'prop-types';

const ImageOption = props => {
  const { classes } = props;

  return <div>ImageOption</div>;
};

ImageOption.propTypes = {
  classes: PropTypes.object.isRequired
};

export default ImageOption;
