import React from 'react';
import PropTypes from 'prop-types';

const RecipeRoute = props => {
  const { classes } = props;

  return <div>RecipeRoute</div>;
};

RecipeRoute.propTypes = {
  classes: PropTypes.object.isRequired
};

export default RecipeRoute;
